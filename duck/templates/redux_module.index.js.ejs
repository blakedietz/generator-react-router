import <%= camelName %>Reducer, {
  actionTypes,
  actionCreators,
  thunks,
  selectors,
} from './<%= snakeName %>';

export default <%= camelName %>Reducer;
export {
  actionTypes,
  actionCreators,
  thunks,
  selectors,
};