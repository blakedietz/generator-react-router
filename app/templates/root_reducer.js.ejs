/**
 * This is the root reducer for the module for which all reducers under the current module will be combined into
 * a single reducer and exported. Note that you can call combineReducers on reducers that have already been combined, so
 * if there are plans to use this store at a higher level that is a viable option.
 */

import { combineReducers } from 'redux';

/**
 * Note here that each reducer name is mapped to the corresponding state key in redux.
 */
export default combineReducers({
  // Your reducers go here
  // reduerStateName: reducer,
});
