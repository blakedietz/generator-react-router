import React from 'react';
import renderer from 'react-test-renderer';
import <%= pascalName %> from '../index';

// This is a basic snapshot test for component rendering.
// Read more about jest tests here https://facebook.github.io/jest/docs/en/using-matchers.html#content
describe(<<%= pascalName %> />.name, () => {
  it('Renders correctly', () => {
    const tree = renderer.create(
    <<%= pascalName %> />
    ).toJSON();

    expect(tree).toMatchSnapshot();
  });
});

